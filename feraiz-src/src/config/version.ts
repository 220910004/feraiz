export const APP_VERSION = '1.1.0';
export const RULESET_VERSION = '2026.04-faraid-core';
export const EXPLANATION_VERSION = '1.0.0';
export const BUILD_TARGET = 'single-file-html';
export const LAST_VERIFIED_AT = '2026-04-02';

export const VERSION_INFO = {
  appVersion: APP_VERSION,
  rulesetVersion: RULESET_VERSION,
  explanationVersion: EXPLANATION_VERSION,
  buildTarget: BUILD_TARGET,
  lastVerifiedAt: LAST_VERIFIED_AT,
} as const;
